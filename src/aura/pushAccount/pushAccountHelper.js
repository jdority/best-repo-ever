({
    validateAccountForm: function(component) {
        var validAccount = true;
          console.log('validateAccountForm ' );
        /*
        Duplicate check logic would go here
        
        */
        return(validAccount);
	}
    }
})